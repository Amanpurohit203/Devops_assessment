# DB backups - these are local dump files, not source code, and can get large
backups/

# vim leftover swap files if a session crashes
*.swp
*.swo

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan.binary
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc